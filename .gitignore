node_modules/
dist/
web/dist/
web/node_modules/
*.db
artifacts/qa/browser-quality/

# local tooling (not part of the published project)
.gitnexus/
.playwright-mcp/
CLAUDE.md
AGENTS.md
.claude/

# review evidence: screenshots agents attach to completion mails. Untracked source hiding
# among these once broke main (an unnoticed driver was never committed), so they are ignored
# and `git status` stays a signal.
/*.png
/*.jpeg
/*.jpg
